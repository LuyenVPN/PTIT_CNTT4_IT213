spring:
  application:
    name: bai3

  ai:
    openai:
      api-key: ${OPEN_ROUTER_API_KEY}
      base-url: ${OPEN_ROUTER_BASE_URL}
      chat:
        options:
          model: ${OPEN_ROUTER_MODEL}
          temperature: 0.3
          max-tokens: 2000