spring:
  application:
    name: bai1

  datasource:
    url: jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres
    username: postgres.bnpkowyremnegpruecsf
    password: Luyen@28042006
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 4
      minimum-idle: 2
      idle-timeout: 30000
      connection-timeout: 20000

  ai:
    ollama:
      base-url: http://localhost:11434

      embedding:
        options:
          model: nomic-embed-text

    vectorstore:
      pgvector:
        table-name: vector_store
        dimensions: 768
        initialize-schema: true

logging:
  level:
    com.rikkei.crm.ticketassistant: INFO